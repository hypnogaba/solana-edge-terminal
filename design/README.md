# Design canvas

`Main.dc.html` and `FeedDown.dc.html` are the artboards the terminal page was
drawn from, laid out by `canvas.json`. They are the source.

The published canvas is generated from them and is not committed: it carries
about 2.4 MB of editor payload that nobody reading this repository needs.
